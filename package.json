{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "pnpm run build && pnpm run dev",
    "dev": "turbo run dev",
    "build": "turbo run build-tsplus-watch --parallel",
    "lint": "turbo run lint",
    "postinstall": "tsplus-install"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "eslint": "7.32.0",
    "prettier": "^2.5.1",
    "turbo": "latest",
    "vitest": "^0.18.1",
    "@tsplus/installer": "0.0.127",
    "typescript": "^4.6.3"
  }
}
